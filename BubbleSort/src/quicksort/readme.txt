快速排序算法实现

快速排序算法就是将需要排序的数按一个标准数分成两个部分，即大于标准数和小于标准数的部分，分别从需要
比较的数组的两端开始与标准数进行比较，当小于标准数时，元素放置在左侧，左侧计数下标加1，当元素大于
标准数时，将元素放置在右侧，计数下标减1。当两下标重合后，分组结束，此时左侧为比标准数小的部分，右侧
为比标准数大的部分，对两部分的数进行快速排递归调用即可。注意加上递归条件，即开始下标小于结束下标。